<div class="random-container">
  <div><img src="{{randomBeer.image_url}}" alt="{{randomBeer.name}}"></div>

  <div class="random-content">
    <h2>{{randomBeer.name}}</h2>

    <p class="random-tagline">{{randomBeer.tagline}}</p>

    <p>{{randomBeer.description}}</p>

    <p class="random-pairing"><strong>Food Pairing:</strong></p>




    <img src="{{randomBeers.0.image_url}}" alt="" srcset="">
<p>Description is: {{randomBeers.0.description}}</p>
<ul>
{{#each randomBeers.0.food_pairing}}
<li>{{this}}</li>
{{/each}}
</ul>

    <p class="random-tips"><strong>Brewers Tips:</strong> {{randomBeer.brewers_tips}}</p>
  </div>
</div>